# -*- python -*-
from lsst.sconsUtils import scripts

# Python-only package
scripts.BasicSConstruct(
    "amplifier_images",
    disableCc=True,
    noCfgFile=True,
    versionModuleName="python/lsst/amplifier_images/version.py",
)
